# 应用名称，用于 FastAPI 标题与日志显示
APP_NAME=AI GroupChat Simulator
# 是否开启调试模式，影响日志与热重载
DEBUG=true
# 服务器监听地址，默认绑定本地
HOST=127.0.0.1
# 服务器监听端口
PORT=8000
# 默认参与群聊的角色列表，使用逗号分隔
DEFAULT_ROLES=贤者,剑士,魔导师
# 每个角色生成的句子数量
REPLY_SENTENCES_PER_ROLE=2
# 随机种子，确保生成结果可重复
SEED=42
