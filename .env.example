# 应用名称，用于 FastAPI 标题与日志显示
APP_NAME=AI GroupChat Simulator
# 是否开启调试模式，影响日志与热重载
DEBUG=true
# 服务器监听地址，默认绑定本地
HOST=127.0.0.1
# 服务器监听端口
PORT=8000
# 每个角色生成的句子数量
REPLY_SENTENCES_PER_ROLE=2
# 随机种子，确保生成结果可重复
SEED=42
# 世界年份，可覆盖默认 1024
WORLD_YEAR=1024
# 当前季节，可选：春/夏/秋/冬
WORLD_SEASON=春
# 队伍默认所在地点
DEFAULT_LOCATION=格兰王都
# 可选：使用 JSON 字符串覆盖六人设
# PERSONAS_JSON=
# 是否启用外部 LLM（默认关闭）
USE_EXTERNAL_LLM=false
# 外部 LLM 提供商标识
LLM_PROVIDER=openai
# OpenAI API 密钥（启用外部 LLM 时必填）
OPENAI_API_KEY=
# OpenAI Base URL（如需代理可配置）
OPENAI_BASE_URL=
